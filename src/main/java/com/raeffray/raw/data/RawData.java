package com.raeffray.raw.data;

public interface RawData {
	
	/**
	 * All properties are following the same translink name pattern xxx_xxx
	 * 
	 * for the json properties, the idea is get rid of all foreign keys, once 
	 * the are going to be graph relationship. The only one that will still
	 * are those ones for which we don't have yet the missing information.
	 * 
	 * **/

}
